---
title: "Markdown 语法测试"
date: "2025-11-26"
description: "测试所有常见的 Markdown 和 GFM 语法渲染效果"
---

# 一级标题

## 二级标题

### 三级标题

#### 四级标题

##### 五级标题

###### 六级标题

---

## 文本样式

这是普通文本。

**这是粗体文本**

*这是斜体文本*

***这是粗斜体文本***

~~这是删除线文本~~

这是 `行内代码` 的样式。

> 这是引用文本
>
> 可以有多行
>
> > 还可以嵌套引用

---

## 列表

### 无序列表

- 项目一
- 项目二
  - 嵌套项目 A
  - 嵌套项目 B
    - 更深层嵌套
- 项目三

### 有序列表

1. 第一项
2. 第二项
   1. 嵌套第一项
   2. 嵌套第二项
3. 第三项

### 任务列表 (GFM)

- [x] 已完成的任务
- [x] 这个也完成了
- [ ] 未完成的任务
- [ ] 另一个待办事项

---

## 链接和图片

### 链接

[普通链接](https://github.com)

[带标题的链接](https://github.com "GitHub 官网")

自动链接: https://github.com

邮箱链接: [example@email.com](mailto:example@email.com)

### 图片

![Alt 文本](https://via.placeholder.com/300x150/1e293b/64748b?text=测试图片)

带链接的图片:

[![图片链接](https://via.placeholder.com/200x100/0f172a/94a3b8?text=点击我)](https://github.com)

---

## 代码块

### JavaScript

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
  return {
    message: "Welcome",
    timestamp: Date.now()
  };
}

const result = greet("World");
```

### TypeScript

```typescript
interface User {
  id: number;
  name: string;
  email?: string;
}

const getUser = async (id: number): Promise<User> => {
  const response = await fetch(`/api/users/${id}`);
  return response.json();
};
```

### Python

```python
def fibonacci(n: int) -> list[int]:
    """生成斐波那契数列"""
    if n <= 0:
        return []
    sequence = [0, 1]
    while len(sequence) < n:
        sequence.append(sequence[-1] + sequence[-2])
    return sequence[:n]

print(fibonacci(10))
```

### Bash

```bash
#!/bin/bash
echo "Hello World"
git status
git add .
git commit -m "feat: 新功能"
```

### JSON

```json
{
  "name": "my-project",
  "version": "1.0.0",
  "dependencies": {
    "react": "^19.0.0",
    "next": "^16.0.0"
  }
}
```

### 无语言指定

```
这是没有指定语言的代码块
会显示为纯文本
```

---

## 表格 (GFM)

### 基本表格

| 姓名 | 年龄 | 职业 |
| ---- | ---- | ---- |
| 张三 | 25 | 工程师 |
| 李四 | 30 | 设计师 |
| 王五 | 28 | 产品经理 |

### 对齐方式

| 左对齐 | 居中对齐 | 右对齐 |
| :--- | :---: | ---: |
| 内容 | 内容 | 内容 |
| 更长的内容 | 更长的内容 | 更长的内容 |
| 短 | 短 | 短 |

### 复杂表格

| 功能 | 描述 | 状态 | 备注 |
| --- | --- | :---: | --- |
| 用户认证 | 登录/注册功能 | ✅ | 已上线 |
| 数据导出 | 支持 CSV/JSON | ⏳ | 开发中 |
| 深色模式 | 自动切换主题 | ❌ | 计划中 |

---

## 脚注 (GFM)

这是一段包含脚注的文本[^1]。

这里还有另一个脚注[^note]。

[^1]: 这是第一个脚注的内容。

[^note]: 这是一个命名脚注，可以包含更多内容。

---

## 分隔线

上面的内容

---

下面的内容

***

另一种分隔线

___

第三种分隔线

---

## 转义字符

\*这不是斜体\*

\`这不是代码\`

\# 这不是标题

---

## HTML 混合

<details>
<summary>点击展开详情</summary>

这是隐藏的内容！

- 可以包含列表
- 还有其他 Markdown 语法

</details>

<kbd>Ctrl</kbd> + <kbd>C</kbd> 复制

这是<sup>上标</sup>和<sub>下标</sub>

<mark>高亮文本</mark>

---

## 数学公式 (KaTeX)

行内公式: $E = mc^2$

块级公式:

$$
\sum_{i=1}^{n} x_i = x_1 + x_2 + \cdots + x_n
$$

更多例子：

$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

$$
\frac{\partial f}{\partial x} = \lim_{h \to 0} \frac{f(x+h) - f(x)}{h}
$$

---

## Emoji

如果支持 emoji 简写: :smile: :rocket: :heart:

直接使用 emoji: 😀 🚀 ❤️ ✨ 🎉

---

## 总结

以上就是常见的 Markdown 语法测试，包括：

1. 标题 (h1-h6)
2. 文本样式 (粗体、斜体、删除线)
3. 列表 (有序、无序、任务)
4. 链接和图片
5. 代码块 (多种语言)
6. 表格 (GFM)
7. 引用
8. 分隔线
9. HTML 混合内容
10. 脚注